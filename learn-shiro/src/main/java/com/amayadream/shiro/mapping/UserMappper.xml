<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.amayadream.shiro.dao.IUserDao">
    <resultMap id="BaseResultMap" type="com.amayadream.shiro.pojo.User">
        <id column="USERID" property="userid" jdbcType="VARCHAR"/>
        <result column="PASSWORD" property="password" jdbcType="VARCHAR"/>
        <result column="ORGANIZATION_ID" property="organization_id" jdbcType="VARCHAR"/>
        <result column="ROLE_IDS" property="role_ids" jdbcType="VARCHAR"/>
        <result column="SALT" property="salt" jdbcType="VARCHAR"/>
        <result column="LOCKED" property="locked" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        USERID, PASSWORD, ORGANIZATION_ID, ROLE_IDS, SALT, LOCKED
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM
        (
        SELECT A.*,ROWNUM RN
        FROM
        (
        SELECT * FROM USERS
        ORDER BY USERID DESC
        ) A
        )
        WHERE RN BETWEEN #{start} and #{end}
    </select>

    <select id="selectAllNoPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM USERS
    </select>

    <select id="selectByUserid" resultMap="BaseResultMap" parameterType="string">
        SELECT
        <include refid="Base_Column_List"/>
        FROM USERS
        WHERE USERID = #{userid, jdbcType=VARCHAR}
    </select>

    <select id="count" resultType="com.amayadream.shiro.pojo.User">
        SELECT COUNT(*) USERID FROM USERS;
    </select>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.amayadream.shiro.pojo.User">
        INSERT INTO USERS (USERID, PASSWORD, ORGANIZATION_ID, ROLE_IDS, SALT, LOCKED)
        VALUES (#{userid, jdbcType=VARCHAR}, #{password, jdbcType=VARCHAR},
        #{organization_id, jdbcType=VARCHAR}, #{role_ids, jdbcType=INTEGER},
        #{salt, jdbcType=INTEGER}, #{locked, jdbcType=INTEGER})
    </insert>

    <!-- 更新用户 -->
    <update id="update" parameterType="com.amayadream.shiro.pojo.User">
        UPDATE USERS
        <set>
            <if test="password != null and password != ''">
                PASSWORD = #{password, jdbcType=VARCHAR},
            </if>
            <if test="organization_id != null">
                ORGANIZATION_ID = #{organization_id, jdbcType=VARCHAR},
            </if>
            <if test="role_ids != null">
                ROLE_IDS = #{role_ids, jdbcType=INTEGER},
            </if>
            <if test="salt != null and salt != ''">
                SALT = #{salt, jdbcType=INTEGER},
            </if>
            <if test="locked != null and locked != ''">
                LOCKED = #{locked, jdbcType=INTEGER},
            </if>
        </set>
        WHERE USERID = #{userid, jdbcType=VARCHAR}
    </update>

    <delete id="delete" parameterType="string">
        DELETE FROM USERS WHERE USERID = #{userid, jdbcType=VARCHAR}
    </delete>

</mapper>